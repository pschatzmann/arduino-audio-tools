# Play with pitch control

Example using PitchedAudioStream to play mp3 files at a different pitch

It will play all mp3 files in the current working directory - usually cmake-build-debug folder

```
mkdir build
cd build
cmake ..
make
```

